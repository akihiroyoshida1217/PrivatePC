[Unit]
Description=clamav Update with Docker
After=docker.service

[Service]
ExecStart=/usr/local/bin/docker-compose -f /opt/docker-image/clamav-package/docker-compose.yml run --rm freshclam
Type=simple

[Install]
WantedBy=multi-user.target
